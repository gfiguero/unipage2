{% extends 'UniAdminBundle:Base:base.html.twig' %}
{% use 'UniAdminBundle:Base:navbar.html.twig' %}
{% use 'UniAdminBundle:Base:sidebar.html.twig' %}
{% use 'UniAdminBundle:Base:icons.html.twig' %}
{% use 'UniAdminBundle:Base:flashbag.html.twig' %}

{% trans_default_domain 'UniAdminBundle' %}

{% block title %}{{ 'socialmediaavailable.show.title'|trans({'%id%':socialMediaAvailable.id}) }}{% endblock %}

{% block body %}
    <div id="page-wrapper">
        <div class="row" id="page-header">
            <div class="col-lg-12">
                <h1 class="page-header">{% block icons %}{{ parent() }}{% endblock %}{{ 'socialmediaavailable.show.title'|trans({'%id%':socialMediaAvailable.id}) }}</h1>
                {% block flashbag %}{{ parent() }}{% endblock %}
            </div>
        </div>
        <!-- /#page-header -->

        <div class="row" id="page-toolbar">
            <div class="col-lg-12">
                <div class="btn-toolbar">
                    <a type="button" class="btn btn-default" href="{{ path('admin_socialmediaavailable_index') }}">{{ icon('list fa-fw') }}&nbsp;{{ 'socialmediaavailable.index.link'|trans }}</a>
                    <a type="button" class="btn btn-primary" href="{{ path('admin_socialmediaavailable_edit', { 'id': socialMediaAvailable.id }) }}">{{ icon('edit fa-fw') }}&nbsp;{{ 'socialmediaavailable.edit.link'|trans }}</a>
                    <button type="button" class="btn btn-danger" data-toggle="modal" data-target="#modalDelete">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'socialmediaavailable.delete.link'|trans }}</button>
                </div>
            </div>
        </div>
        <!-- /#page-toolbar -->

        <div class="row" id="page-show">
            <div class="col-lg-12">
                <div class="panel panel-default">
                    <div class="panel-heading">
                        <h2 class="panel-title">{{ 'socialmediaavailable.show.data'|trans }}</h2>
                    </div>
                    <div class="panel-body">
                        <dl class="dl-horizontal">
                            <dt>{{ 'socialmediaavailable.form.id'|trans }}</dt>
                                <dd>{% if socialMediaAvailable.id %}{{ socialMediaAvailable.id }}{% endif %}</dd>
                            <dt>{{ 'socialmediaavailable.form.name'|trans }}</dt>
                                <dd>{% if socialMediaAvailable.name %}{{ socialMediaAvailable.name }}{% endif %}</dd>
                            <dt>{{ 'socialmediaavailable.form.icon'|trans }}</dt>
                                <dd>{% if socialMediaAvailable.icon %}{{ socialMediaAvailable.icon }}{% endif %}</dd>
                            <dt>{{ 'socialmediaavailable.form.hexcolor'|trans }}</dt>
                                <dd>{% if socialMediaAvailable.hexcolor %}{{ socialMediaAvailable.hexcolor }}{% endif %}</dd>                        </dl>
                    </div>
                </div>
            </div>
        </div>
        <!-- /#page-show -->

        <div id="page-modal">
            <div class="modal fade" id="modalDelete" tabindex="-1" role="dialog" aria-labelledby="myModalLabel">
                <div class="modal-dialog modal-md" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">{{ icon('times') }}</button>
                            <h4 class="modal-title" id="myModalLabel">{{ 'socialmediaavailable.delete.title'|trans }}</h4>
                        </div>
                        <div class="modal-body">
                            <div class="row">
                                <div class="col-xs-2 text-center">{{ icon('exclamation-triangle fa-5x') }}</div>
                                <div class="col-xs-10"><p>{{ 'socialmediaavailable.delete.message'|trans }}</p></div>
                            </div>
                        </div>
                        <div class="modal-footer">
                            {{ form_start(deleteForm) }}
                                <button type="submit" class="btn btn-danger pull-right">{{ icon('trash-o fa-fw') }}&nbsp;{{ 'socialmediaavailable.delete.submit'|trans }}</button>
                            {{ form_end(deleteForm) }}
                        </div>
                    </div>
                </div>
            </div>

        </div>
        <!-- /#page-modal -->

    </div>
    <!-- /#page-wrapper -->
{% endblock %}

